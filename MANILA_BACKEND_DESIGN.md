# 马尼拉电子版后端实现设计说明书

本文档基于 [MANILA_RULES.md](./MANILA_RULES.md) 设计当前阶段的后端实现方案。

当前阶段目标：

- 先实现后端，不实现交互界面。
- 后端提供稳定接口，未来可接入人类玩家前端和 AI 玩家程序。
- 规则以 4 人局为唯一目标，不考虑 3 人或 5 人变体。
- 先设计，不写代码。

当前已确认的关键口径：

- 使用 Go 实现权威规则引擎和后端服务。
- Python AI 通过接口接入，不在 Python 侧重写规则。
- 不同游戏局并行处理，同一游戏局内部串行处理。
- 放置阶段不允许放弃，每次轮到玩家必须放置 1 个同伙。
- 玩家和 AI 不主动抵押或赎回股份，强制支付现金不足时由系统自动抵押。
- 破产状态由系统自动进入，本轮内不可解除；破产玩家只能作为偷渡客上船并花掉所有剩余现金。
- 若破产玩家无可用货船空位，则自动场外偷渡到 ID 最小的出航船，不占位置。
- 海盗登船后变为普通船员，海盗船上剩余棋子自动决定新船长。
- 船 ID 按收益从低到高固定为 1 到 4，对应收益 18、24、30、36。

## 1. 技术选型建议

推荐使用 Go 作为当前项目主语言。

理由：

- Go 的类型系统足够清晰，适合表达游戏状态、阶段、动作和结算结果。
- Go 的标准库 HTTP 支持成熟，后续提供 REST、WebSocket 或本地进程接口都比较轻。
- Go 编译部署简单，适合把后端做成一个单独服务，前端、人类客户端和 AI 客户端都可以通过接口接入。
- 相比 Python，Go 在长期维护大型状态机时更容易避免隐式类型和运行时错误。
- 相比 C++，Go 的开发成本和内存管理复杂度更低。
- 相比 Java，Go 的项目结构和运行部署更轻。

备选说明：

- Python 适合快速验证 AI 策略，但不建议作为主规则引擎语言；未来可以让 Python AI 通过 HTTP 或 WebSocket 接入 Go 后端。
- C++ 性能足够但复杂度偏高，本项目规则计算量很小，不需要为性能付出额外复杂度。
- Java 也可行，但对当前桌游后端来说略显重。

## 2. 总体架构

后端分为三层：

```text
接口层 API
  |
应用层 Game Service
  |
规则引擎 Rules Engine
```

### 2.1 接口层 API

职责：

- 创建游戏。
- 查询游戏状态。
- 提交玩家动作。
- 返回合法动作。
- 推送或返回游戏事件。
- 支持人类前端和 AI 程序用同一套协议交互。

接口层不直接修改规则状态，只把请求转换为应用层命令。

### 2.2 应用层 Game Service

职责：

- 管理多局游戏实例。
- 校验玩家身份和座位。
- 调用规则引擎执行动作。
- 保存游戏快照和事件日志。
- 控制随机数种子。
- 在需要时触发自动步骤，例如无人出价后的港务长归属、结算、升值和进入下一轮。

### 2.3 规则引擎 Rules Engine

职责：

- 保存完整游戏状态。
- 根据当前阶段生成合法动作。
- 校验动作是否合法。
- 执行动作并产生状态变更。
- 产生事件日志。
- 保证同样初始状态、同样随机种子、同样动作序列得到同样结果。

规则引擎应尽量做成纯逻辑模块，不依赖 HTTP、数据库、前端或 AI。

## 3. 核心设计原则

### 3.1 所有参与者都提交动作

人类玩家和 AI 玩家在后端看来都是同一种 Actor。

后端不关心动作来自按钮、网页、命令行还是 AI 程序，只关心：

- 哪个游戏。
- 哪个玩家。
- 当前阶段。
- 提交了什么动作。
- 动作是否合法。

这样未来可以自由替换前端，也可以让 AI 与人类混合对局。

### 3.2 后端生成合法动作

客户端不应自己推导完整规则。客户端只负责展示后端返回的合法动作，并提交其中一个。

理由：

- 规则较复杂，尤其是竞拍、贷款、海盗、领航员、保险赔付。
- 如果前端和 AI 各自实现规则，容易出现口径不一致。
- 后端统一生成合法动作，可以让 AI 直接基于合法动作列表决策。

### 3.3 状态机驱动

游戏应由明确阶段驱动。每个阶段只允许特定动作。

理由：

- 《马尼拉》规则中同一玩家在不同阶段可做的事完全不同。
- 海盗、领航员、保险赔付等规则依赖精确时机。
- 用状态机可以避免“某动作在错误时间被提交”的问题。

### 3.4 事件日志优先

每次动作和自动结算都应产生日志事件。

事件日志用途：

- 前端回放动画。
- AI 分析对局历史。
- 调试规则错误。
- 保存与恢复游戏。
- 未来实现观战或复盘。

## 4. 主要领域模型

### 4.1 Game

一局游戏的根状态。

字段建议：

- `gameId`：游戏唯一 ID。
- `status`：等待开始、进行中、已结束。
- `roundNumber`：当前轮次，从 1 开始。
- `phase`：当前阶段。
- `players`：4 名玩家状态。
- `goods`：4 种货物状态。
- `ships`：当前轮出航船状态。
- `board`：港口、船坞、海盗、领航员、保险等位置状态。
- `bank`：港口钱箱或公共钱池状态。
- `currentTurn`：当前需要行动的玩家。
- `harborMaster`：本轮港务长。
- `previousHarborMaster`：上一任港务长。
- `auction`：当前竞拍状态。
- `round`：当前轮临时状态。
- `randomSeed`：随机种子。
- `eventSeq`：事件序号。
- `events`：事件日志。

### 4.2 Player

玩家状态。

字段建议：

- `playerId`：玩家编号，固定为 1 到 4。
- `cash`：现金。
- `shares`：持有股份数量，按货物分类。
- `mortgagedShareCount`：已抵押股份总张数，不记录具体抵押哪一种货物。
- `availableAccomplices`：可用同伙数量。
- `placedAccomplices`：本轮已放置同伙。
- `bankruptThisRound`：本轮是否已经进入破产状态，进入后本轮不可解除。
- `actorType`：人类、AI、测试脚本等。
- `connected`：是否在线，主要给接口层使用。

### 4.3 Goods

货物状态。

字段建议：

- `goodsType`：人参、肉豆蔻、丝绸、玉石。
- `marketValueIndex`：价值轨道索引。
- `marketValue`：当前价值，可由索引计算得到。
- `sharesRemaining`：公开供应中剩余股份数。
- `shipPayout`：货船到港总收益。
- `boardingCosts`：货船同伙费用格。

价值轨道固定为：

```text
[0, 5, 10, 20, 30]
```

### 4.4 Ship

当前轮出航船状态。

字段建议：

- `shipId`：船 ID，按货船总收益从低到高排列。
- `goodsType`：运输货物。
- `routeIndex`：航线编号。
- `position`：当前位置。
- `status`：航行中、已到港、进船坞、被海盗处理中。
- `occupants`：货船上同伙列表。
- `stowaways`：无空位偷渡到该船的同伙列表，不占货船格。
- `nextBoardingSlot`：下一个可用费用格索引。
- `portSlot`：最终进入的港口格，若有。
- `dockSlot`：最终进入的船坞格，若有。
- `lootedByPirates`：是否被海盗掠夺。
- `formerPiratesAsCrew`：本轮从海盗船登上该船、已转为普通船员的同伙记录。

船 ID 固定对应：

| 船 ID | 货物 | 到港总收益 |
| ---: | --- | ---: |
| 1 | 人参 | 18 |
| 2 | 肉豆蔻 | 24 |
| 3 | 丝绸 | 30 |
| 4 | 玉石 | 36 |

由于船 ID 已按收益从低到高排列，需要按货船收益打破平局时，可以直接比较 `shipId`。

### 4.5 Board Positions

版图位置分为以下类型：

- 货船格。
- 港口 A/B/C。
- 船坞 A/B/C。
- 海盗船长。
- 海盗副手。
- 小领航员。
- 大领航员。
- 保险。

统一字段建议：

- `positionId`：位置 ID。
- `positionType`：位置类型。
- `cost`：放置费用。
- `reward`：成功收益，若有。
- `occupant`：占用该位置的玩家同伙，若有。

货船格比较特殊，因为同一艘船有多个按顺序使用的费用格；可将它建模为船内 slots。

海盗船位置需要额外记录动态身份：

- 棋子仍在海盗船上时，才拥有海盗身份。
- 当前海盗船上排序第一的棋子是海盗船长。
- 原船长登船后，如果副手仍在海盗船上，副手自动晋升为新船长。
- 已登船的原海盗应转入目标货船 occupants，不再参与海盗决策。

### 4.6 Auction

竞拍状态。

字段建议：

- `startPlayer`：本轮竞拍起始玩家。
- `currentBid`：当前最高价。
- `highestBidder`：当前最高出价玩家。
- `passedPlayers`：已放弃竞拍玩家集合。
- `activePlayer`：当前需要竞拍行动的玩家。
- `hasAnyBid`：本轮是否有人出价。

### 4.7 Round State

当前轮临时状态。

字段建议：

- `selectedGoods`：本轮出航的 3 种货物。
- `excludedGoods`：本轮未出航货物。
- `placementStep`：第几次放置阶段，1 到 3。
- `movementStep`：第几次移动阶段，1 到 3。
- `diceResults`：每次移动的骰子结果。
- `arrivedShips`：已到港船列表。
- `dockedShips`：已进船坞船列表。
- `pendingPirateBoarding`：第 2 次移动后待处理海盗登船的船。
- `pendingPirateLooting`：第 3 次移动后待处理海盗掠夺的船。
- `pendingNavigatorAction`：第 3 次移动前待处理领航员行动。
- `roundEnded`：本轮是否已完成结算。

## 5. 游戏阶段设计

建议使用枚举表示阶段。

### 5.1 阶段列表

```text
NotStarted
Auction
HarborMasterBuyShare
HarborMasterSelectGoods
HarborMasterSetShips
Placement
RollDiceAndMove
PirateBoarding
NavigatorAction
PirateLooting
RoundSettlement
MarketAdvance
GameEnd
```

说明：

- `Auction` 允许竞拍放弃；这是竞拍机制的一部分。
- `Placement` 不允许放弃；轮到玩家时必须通过 `PlaceAccomplice` 放置 1 个同伙。
- `PirateBoarding`、`NavigatorAction` 允许跳过效果，因为这些动作代表是否发动特殊能力，不属于放置阶段。
- `RoundSettlement` 和 `MarketAdvance` 为自动阶段，不接收玩家动作。

### 5.2 标准轮次流转

```text
Auction
  -> HarborMasterBuyShare
  -> HarborMasterSelectGoods
  -> HarborMasterSetShips
  -> Placement 1
  -> RollDiceAndMove 1
  -> Placement 2
  -> RollDiceAndMove 2
  -> PirateBoarding, if needed
  -> Placement 3
  -> NavigatorAction, if needed
  -> RollDiceAndMove 3
  -> PirateLooting, if needed
  -> RoundSettlement
  -> MarketAdvance
  -> GameEnd or next Auction
```

如果某个条件阶段没有需要处理的动作，应自动跳过。

示例：

- 第 2 次移动后没有船停在 13，跳过 PirateBoarding。
- 没有人占领航员位置，跳过 NavigatorAction。
- 第 3 次移动后没有船停在 13 或没有海盗，跳过 PirateLooting。

## 6. 动作协议设计

### 6.1 Action 基本结构

所有玩家动作统一为 Action。

字段建议：

- `actionId`：客户端生成或服务端生成的动作 ID，用于幂等。
- `gameId`：游戏 ID。
- `playerId`：玩家 ID。
- `type`：动作类型。
- `payload`：动作参数。
- `clientSeq`：客户端序号，可选。

### 6.2 动作类型

建议动作类型如下：

| 动作 | 阶段 | 说明 |
| --- | --- | --- |
| `Bid` | Auction | 出价 |
| `PassBid` | Auction | 放弃竞拍 |
| `BuyShare` | HarborMasterBuyShare | 港务长购买股份 |
| `SkipBuyShare` | HarborMasterBuyShare | 港务长不买股份 |
| `SelectGoods` | HarborMasterSelectGoods | 选择本轮 3 种出航货物 |
| `SetShipStarts` | HarborMasterSetShips | 设置 3 艘船起点 |
| `PlaceAccomplice` | Placement | 放置同伙 |
| `PirateBoard` | PirateBoarding | 海盗登船 |
| `PirateSkipBoard` | PirateBoarding | 海盗不登船 |
| `NavigatorMove` | NavigatorAction | 领航员移动船 |
| `NavigatorSkip` | NavigatorAction | 领航员不移动 |
| `PirateChooseDestination` | PirateLooting | 海盗船长选择被掠夺船去港口或船坞 |

`PlaceAccomplice` 的 payload 应至少包含：

- `positionType`：货船、港口、船坞、海盗、领航员、保险。
- `targetId`：具体目标，例如 shipId、港口格、船坞格或特殊位置 ID。
- `payment`：客户端可传预期支付金额用于校验；最终支付金额以后端规则为准。

破产偷渡仍使用 `PlaceAccomplice`，但合法动作列表中只返回唯一目标。后端执行时自动将支付金额设为玩家当前全部现金。

如果是无空位偷渡，合法动作中应标记：

- `isStowaway`：true。
- `occupiesSlot`：false。
- `targetId`：ID 最小的本轮出航船。

### 6.3 自动动作

部分步骤不需要玩家提交动作，由后端自动执行：

- 无人出价时确定免费港务长。
- 玩家满足条件时自动进入破产状态。
- 掷骰并移动船。
- 根据规则确定船到港或进船坞。
- 现金不足时自动抵押股份。
- 支付收益。
- 保险赔付。
- 货物升值。
- 回收同伙。
- 进入下一轮。

这些自动步骤也应产生事件，方便客户端展示。

自动动作不得绕过事件日志。即使没有玩家输入，也要追加对应事件，保证前端、AI 和复盘都能解释状态为什么变化。

## 7. 合法动作生成

后端应提供“查询当前合法动作”的能力。

### 7.1 玩家查询合法动作

返回内容建议：

- 当前是否轮到该玩家。
- 如果不是，返回等待原因。
- 如果是，返回可选动作列表。
- 每个动作包含动作类型、参数范围、支付成本、预期影响摘要。
- 如果当前阶段是 `Placement` 且轮到该玩家，动作列表必须只包含 `PlaceAccomplice`。
- `Placement` 阶段返回空动作列表表示规则状态异常，不应作为正常游戏流程。

示例信息：

```text
当前阶段：Placement
当前玩家：2
合法动作：
- PlaceAccomplice 到 人参船，费用 1
- PlaceAccomplice 到 港口 B，费用 3
- PlaceAccomplice 到 海盗船长，费用 5
```

破产玩家有空位可偷渡示例：

```text
当前阶段：Placement
当前玩家：2
玩家状态：本轮已破产
合法动作：
- PlaceAccomplice 到 船 ID 1，偷渡客，支付全部剩余现金
```

破产玩家无空位偷渡示例：

```text
当前阶段：Placement
当前玩家：2
玩家状态：本轮已破产
合法动作：
- PlaceAccomplice 到 船 ID 1，场外偷渡，不占位置，支付全部剩余现金
```

### 7.2 AI 使用方式

AI 不需要读完整状态后自己推导规则。推荐流程：

1. AI 拉取游戏状态。
2. AI 拉取自己的合法动作。
3. AI 根据策略选择一个动作。
4. AI 提交动作。
5. 后端校验并推进游戏。

复杂 AI 可以基于完整状态做搜索或模拟；简单 AI 可以只基于合法动作和启发式评分。

## 8. 接口设计

当前阶段可先使用 REST 接口，后续需要实时推送时再加 WebSocket。

### 8.1 游戏管理接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/games` | 创建新游戏 |
| `GET` | `/games/{gameId}` | 查询游戏摘要 |
| `GET` | `/games/{gameId}/state` | 查询完整游戏状态 |
| `GET` | `/games/{gameId}/events` | 查询事件日志 |
| `POST` | `/games/{gameId}/start` | 开始游戏 |

### 8.2 玩家动作接口

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `GET` | `/games/{gameId}/players/{playerId}/actions` | 查询该玩家合法动作 |
| `POST` | `/games/{gameId}/actions` | 提交动作 |

### 8.3 AI 接入接口

AI 可以直接使用玩家动作接口，不需要专门接口。

如果未来要让后端托管 AI，可增加：

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/games/{gameId}/players/{playerId}/attach-ai` | 将座位交给后端 AI |
| `POST` | `/games/{gameId}/players/{playerId}/detach-ai` | 取消后端 AI 托管 |

当前阶段建议先不做托管 AI，只保证外部 AI 程序可以通过接口提交动作。

### 8.4 调试接口

开发阶段建议保留调试接口，但正式环境关闭。

| 方法 | 路径 | 说明 |
| --- | --- | --- |
| `POST` | `/debug/games/{gameId}/snapshot` | 保存当前快照 |
| `POST` | `/debug/games/{gameId}/load-snapshot` | 载入快照 |
| `POST` | `/debug/games/{gameId}/set-seed` | 设置随机种子 |

理由：

- 规则复杂，尤其是海盗、保险和贷款。
- 可复现局面能大幅降低调试成本。

## 9. 随机数设计

游戏中随机来源包括：

- 初始随机发放股份。
- 每次掷骰。

必须由后端统一生成随机数。

建议：

- 创建游戏时生成或指定 `randomSeed`。
- 规则引擎使用可复现的伪随机数。
- 每次随机结果都写入事件日志。

理由：

- AI 对战和测试需要可复现。
- 前端不能参与随机，否则容易作弊或导致状态不一致。
- 事件日志记录骰子结果后，复盘不需要重新随机。

## 10. 复杂规则处理设计

### 10.1 竞拍与支付能力

竞拍出价上限不只是现金，还包括可抵押股份带来的贷款能力。

设计建议：

- 提供 `MaxPayable(player)` 规则函数。
- 出价时校验 `bid <= MaxPayable(player)`。
- 真正支付时，如果现金不足，系统自动抵押股份补足现金。

复杂点：

规则层面需要明确自动抵押顺序，但抵押哪种股份不影响最终财富。因为抵押股份仍属于玩家，终局会按市值计分并扣除未赎回贷款；如果玩家终局仍资不抵债，允许得到负分。

建议处理：

- 不提供主动抵押或主动赎回动作。
- 强制支付时，如果现金不足，由规则引擎按固定顺序自动抵押未抵押股份。
- 自动抵押顺序采用系统最方便的稳定顺序，例如按货物枚举顺序。
- 每次自动抵押都记录事件，说明抵押了哪些股份、获得多少现金、用于支付什么费用。

理由：

主动抵押从贪心角度没有必要，提前抵押只会增加现金而不会创造额外收益。由于抵押哪种股份不会改变终局财富，自动处理不会影响玩家策略，也可以避免支付过程暂停状态机。

### 10.2 强制放置同伙

放置阶段不允许放弃操作。

设计建议：

- 每次轮到玩家在 Placement 阶段行动时，合法动作列表必须只包含放置动作。
- 如果玩家未破产，返回所有普通合法放置动作。
- 如果玩家已破产，返回唯一偷渡客放置动作。
- 如果破产玩家没有可用货船空位，返回场外偷渡动作，目标为 ID 最小的本轮出航船，不占位置。
- 如果玩家未破产且没有任何合法放置位置，视为程序错误。
- 4 人规则下每轮有 3 个放置阶段，每名玩家有 3 个同伙，因此正常情况下每名玩家每轮会放完 3 个同伙。

### 10.3 破产状态判定

破产状态是玩家的本轮状态，由系统自动进入。

判定时机：

- 在放置阶段轮到玩家行动时，生成合法动作前检查。
- 如果玩家已经 `bankruptThisRound == true`，不再重新判断，直接按破产状态生成动作。
- 每轮开始时重置所有玩家的 `bankruptThisRound` 为 false。

进入条件：

- 玩家没有可以抵押的未抵押股份。
- 玩家当前现金低于当前局面下任何一个可行放置操作的费用。

可行放置操作说明：

- 包括所有当前可合法放置的位置，例如货船、港口、船坞、海盗、领航员、保险。
- 如果存在费用为 0 的可行操作，例如保险位置，则玩家不会进入破产状态。

进入破产状态时，规则引擎应产生 `PlayerBecameBankrupt` 事件。

破产状态效果：

- 本轮内无法解除。
- 即使玩家之后获得现金，本轮仍保持破产状态。
- 破产玩家不能选择普通放置动作。
- 破产玩家只能作为偷渡客放置到指定货船。

### 10.4 多艘船同时到港或进船坞

当前规则确认程序可用最方便的稳定顺序处理。

设计建议：

- 使用固定顺序排序，例如按 `routeIndex` 或 `shipId`。
- 结算事件中明确记录实际顺序。

理由：

即使当前规则认为顺序不影响结果，事件日志仍应记录后端采用的顺序，方便排查争议。

### 10.5 海盗登船

第 2 次移动后，如果船刚好停在 13，海盗可登船，但必须有空格。

设计建议：

- 移动 2 后扫描所有 `position == 13` 且 `status == Sailing` 的船。
- 过滤出仍有货船空格的船。
- 如果没有可登船目标，跳过 PirateBoarding。
- 如果海盗船上有海盗，按当前船长优先的顺序依次给出登船或跳过动作。
- 海盗执行 `PirateBoard` 后，从海盗船位置移除，加入目标货船的 occupants，并立刻变为普通船员。
- 如果被移除的是当前船长，海盗船上剩余的下一名海盗自动成为新船长。

复杂点：

如果多艘船都停在 13，当前海盗可选择哪一艘；如果某艘船没有空格，不应显示为合法目标。原海盗登船后身份会变化，因此第 3 次移动后的掠夺只与仍留在海盗船上的海盗有关。

### 10.6 海盗掠夺后选择去向

第 3 次移动后，船刚好停在 13 且海盗船上仍有海盗，则触发掠夺。

设计建议：

- 先确定所有被掠夺船。
- 支付海盗收益或记录待支付项，收益只分给仍留在海盗船上的海盗。
- 由当前海盗船长为每艘被掠夺船选择进入港口或船坞。
- 如果原海盗船长已在第 2 次移动后登上货船，则他已经是普通船员，不再拥有船长决策权。

建议规则口径：

- 海盗身份跟随海盗船位置，不跟随玩家或原始身份。棋子登船后转为普通船员；海盗船上剩余的最高身份者成为当前船长。后续劫掠、目的地选择和海盗收益只与当前海盗船上的海盗有关。

理由：

这符合当前项目规则口径，也能让第 2 次移动后的登船选择产生真实后果：登船可以争取货船收益，但会放弃后续海盗权力。

### 10.7 领航员

领航员在第 3 次掷骰前行动。

设计建议：

- 如果小领航员存在，先进入小领航员行动。
- 如果大领航员存在，再进入大领航员行动。
- 领航员动作需要校验移动幅度、目标船状态、移动后位置。
- 领航员把船推过 13 时，船立即到港。
- 领航员把船推到 13 时，不触发海盗。

复杂点：

大领航员可以移动 1 艘船最多 2 格，也可以移动 2 艘船各 1 格。

建议把 `NavigatorMove` payload 设计为一组移动指令：

```text
[
  { shipId, delta },
  { shipId, delta }
]
```

校验规则：

- 小领航员最多 1 条指令，delta 只能是 -1、0、1。
- 大领航员可以 1 条指令，delta 为 -2 到 2；也可以 2 条指令，每条 delta 为 -1 或 1。
- 不允许移动已到港船。

### 10.8 保险赔付

保险位置立即获得 10 比索，但结算时承担船坞赔付。

设计建议：

- 放置到保险时立即给玩家加 10。
- 结算船坞收益时，如果有保险员，由保险员支付。
- 保险员本轮其他收益应先到账，再支付赔偿。
- 若现金不足，系统自动抵押股份补足。
- 若仍不足，剩余由港口钱箱支付。

复杂点：

保险赔付依赖收益结算顺序。

建议结算流程：

1. 计算本轮所有应收收益。
2. 先发放保险员自己的非船坞赔付收益。
3. 再处理船坞赔付。
4. 记录保险员实际支付金额和港口钱箱补足金额。

理由：

规则明确保险员可以先收其他收益再赔付，因此不能简单地在船坞结算时立刻扣钱，除非已经保证其他收益到账。

### 10.9 贷款与强制抵押

贷款会影响支付能力，但本项目中贷款完全由系统自动处理。玩家和 AI 不需要主动抵押股份，也不需要主动赎回股份。

规则口径：

- 玩家必须支付但现金不足时，由系统自动抵押股份触发贷款。
- 玩家不能主动抵押股份。
- 玩家不能主动赎回股份。
- 终局时未赎回抵押股份按每张 15 扣分，允许最终财富为负。

复杂点：

强制贷款可能发生在竞拍支付、放置费用、保险赔付中。玩家可能有多种股份可抵押，但抵押哪一种股份不影响最终财富。

建议做法：

- 规则引擎在执行动作时发现需要额外现金，调用自动抵押函数。
- 自动抵押函数按固定顺序抵押股份，每张股份获得 12 比索，直到现金足够支付或没有可抵押股份。
- 如果自动抵押后仍不足，则按具体规则处理。例如保险赔付不足时，剩余由港口钱箱补足。
- 自动抵押不产生玩家待操作阶段，只产生事件日志。

这样能显著简化状态机和接口，也方便 AI 训练，因为支付动作不会被中途打断。

### 10.10 破产时的货船放置

规则允许破产玩家支付所有剩余现金放置到货船，甚至完全免费作为偷渡客。

设计建议：

- 合法动作生成时先调用破产状态判定。
- 如果玩家未破产，则按正常规则生成可支付的放置动作，并在现金不足时自动抵押。
- 如果玩家已破产，则不生成普通放置动作，只生成偷渡客动作。
- 破产放置规则：
  - 如果玩家还有现金，支付所有现金。
  - 如果玩家现金为 0，免费放置。
  - 只能选择当前可用费用最低的货船。
  - 如果有多艘货船费用同为最低，只能选择 ID 最小的船。
  - 如果没有任何可用货船空位，自动偷渡到 ID 最小的本轮出航船。
  - 无空位偷渡不占货船位置。
- 该特殊规则只适用于货船，不适用于港口、船坞、海盗、领航员或保险。

复杂点：

破产放置不是玩家自由少付，而是进入破产状态后的唯一放置方式。

建议实现为一个独立函数：

```text
GetBankruptShipPlacementTarget(game, player) -> shipId or none
```

该函数只在玩家处于破产状态时调用，并返回唯一目标船。

选择逻辑：

1. 如果存在可用货船空位，选择当前可用费用最低的货船；费用并列时选择 ID 最小的船。
2. 如果不存在可用货船空位，选择 ID 最小的本轮出航船，作为场外偷渡目标。

如果本轮没有任何出航船，则视为程序错误。

理由：

这样能保证破产保护不会变成策略性少付，也能让 AI 接口非常简单：破产时后端只返回唯一合法货船目标。

## 11. 结算设计

### 11.1 结算输入

结算开始时应已经确定：

- 每艘船最终去向。
- 哪些船被海盗掠夺。
- 哪些船到港。
- 哪些船进船坞。
- 所有版图位置的同伙。

### 11.2 结算顺序

建议规则引擎内部使用以下顺序：

1. 确定海盗掠夺结果。
2. 计算货船收益。
3. 计算港口收益。
4. 计算船坞收益。
5. 发放非保险赔付收益。
6. 处理保险赔付。
7. 货物升值。
8. 回收同伙。
9. 判断游戏结束。

### 11.3 货船与偷渡收益计算

货船到港时，先按普通船员计算收益：

```text
普通单份收益 = 货船总收益 / 普通船员数量
```

普通船员包括：

- 正常放置在货船费用格上的同伙。
- 第 2 次移动后登船并转为普通船员的原海盗。

场外偷渡者不占货船位置，不参与普通平分，不增加分母。

在普通船员获得收益后，每名场外偷渡者获得一份 `普通单份收益`。

如果一艘船没有普通船员但存在场外偷渡者，当前规则不会自然产生普通单份收益。该情况理论上不应出现，因为无空位偷渡只会在货船已无可用空位时发生；若实现中出现，应视为程序错误。

### 11.4 结算输出

结算应产生一组结构化事件，而不是只改变状态。

事件例子：

- `ShipArrived`
- `ShipDocked`
- `PiratesLootedShip`
- `PlayerReceivedPayout`
- `InsurancePaidDockReward`
- `BankCoveredInsuranceShortfall`
- `GoodsMarketAdvanced`
- `PlayerBecameBankrupt`
- `StowawayPlaced`
- `StowawayPayoutReceived`
- `GameEnded`

## 12. 状态可见性

当前规则中大部分信息都是公开信息。

建议：

- 游戏状态默认对所有玩家公开。
- 随机种子不在游戏进行中暴露给客户端。
- 事件日志公开。
- 调试接口和内部字段只在开发模式开放。

理由：

如果暴露随机种子，理论上客户端可以预测未来骰子。即便当前不是强竞技环境，也应避免这个设计漏洞。

## 13. 数据持久化

当前阶段可以先做内存版，规则稳定后再加持久化。

建议演进：

1. 内存状态 + 事件日志。
2. 文件快照，方便本地调试。
3. SQLite 或 PostgreSQL 持久化。

如果一开始就做持久化，建议保存两类数据：

- 当前快照：快速恢复游戏。
- 事件日志：用于回放和排错。

## 14. 测试设计

规则引擎必须重点测试。

### 14.1 单元测试

重点覆盖：

- 初始化发牌和股权数量。
- 竞拍顺序与无人出价。
- 港务长购买股份价格。
- 起点设置总和为 9。
- 放置同伙合法性。
- 货船费用格顺序。
- 港口和船坞收益。
- 海盗第 2 次移动登船。
- 海盗第 3 次移动掠夺。
- 领航员移动限制。
- 保险赔付和现金不足。
- 贷款和终局扣分。
- 破产状态自动进入、本轮锁定和偷渡客放置。
- 场外偷渡不占货船位置，且不参与普通收益平分。
- 放置阶段没有放弃动作，玩家必须放置同伙。
- 货物升值和游戏结束。
- 并列排名。

### 14.2 剧本测试

使用固定状态和固定骰子，测试完整一轮。

建议至少设计：

- 三船全部到港。
- 三船全部进船坞。
- 一船到港、一船停 13 被海盗、一船进船坞。
- 保险员赔付不足，需要抵押。
- 破产玩家无可用货船空位，场外偷渡到 ID 最小船，并在到港时获得普通单份收益。
- 货物升到 30，触发终局。

### 14.3 随机模拟测试

编写简单随机 AI，从合法动作中随机选择。

用途：

- 连续跑大量对局，发现状态机卡死。
- 验证现金、股份、同伙数量不会出现非法值。
- 验证游戏最终能结束。

## 15. AI 接入设计

AI 可以分为外部 AI 和内置 AI。

当前阶段推荐先支持外部 AI。

### 15.1 外部 AI

外部 AI 作为普通客户端：

1. 查询游戏状态。
2. 查询合法动作。
3. 计算选择。
4. 提交动作。

优点：

- 语言自由，Python、Go、Java 都能写 AI。
- 不影响后端规则引擎。
- 多种 AI 可以并行开发。

### 15.2 内置 AI

后续可以在后端实现简单 AI：

- 随机 AI：测试用。
- 贪心 AI：根据收益期望简单评分。
- 搜索 AI：模拟未来若干步。

内置 AI 应仍然通过规则引擎的合法动作接口行动，不应绕过动作校验。

## 16. 错误处理

接口错误应返回明确原因。

常见错误：

- 游戏不存在。
- 玩家不存在。
- 游戏已结束。
- 当前未轮到该玩家。
- 当前阶段不允许该动作。
- 动作参数非法。
- 现金或贷款能力不足。
- 目标位置已被占用。
- 目标船不在可操作状态。
- 动作版本过期。

建议每个错误包含：

- `errorCode`：稳定错误码。
- `message`：可读说明。
- `details`：可选详细字段。

## 17. 并发与一致性

即使当前没有前端，也应考虑未来多个客户端同时提交动作。

建议：

- 不同游戏局之间允许并行处理请求。
- 同一游戏局内部必须串行处理请求。
- 每局游戏使用独立的动作队列、单局 worker，或 per-game mutex。
- 每个动作带 `expectedEventSeq`，表示客户端基于哪个事件版本提交。
- 如果版本过期，后端拒绝或要求客户端重新拉取状态。

理由：

桌游动作天然是顺序执行的。同一局内串行处理可以保证状态机不会被并发动作打乱；不同局之间没有共享规则状态，应当并行处理，避免一局慢请求阻塞其他对局。

### 17.1 推荐并发模型

建议 Game Service 维护一个按 `gameId` 分片的执行模型：

- 收到请求后先根据 `gameId` 找到对应游戏实例。
- 只锁定或排队该游戏实例。
- 在锁内完成动作校验、状态修改和事件追加。
- 释放锁后返回响应。
- 不同 `gameId` 的请求使用不同锁或不同 worker，因此可以并行执行。

实现方式可以二选一：

| 方案 | 说明 | 适用情况 |
| --- | --- | --- |
| per-game mutex | 每局一个锁，请求线程拿锁后执行动作 | 简单、适合第一版 |
| per-game worker queue | 每局一个动作队列，由该局 worker 顺序消费 | 更适合 WebSocket、AI 批量模拟和长连接 |

第一版推荐 per-game mutex。它实现简单，已经能满足“不同局并行、同局串行”的要求。后续如果需要大量 AI 对局或异步推送，再演进到 per-game worker queue。

## 18. 项目模块建议

如果使用 Go，建议后续代码结构如下：

```text
/cmd/server              后端服务入口
/internal/api            HTTP 接口层
/internal/app            Game Service
/internal/rules          纯规则引擎
/internal/model          领域模型
/internal/store          存储接口与实现
/internal/ai             可选内置 AI
/internal/testscenario   测试剧本
/docs                    文档
```

当前仓库已有规则文档在根目录。后续也可以把文档统一移动到 `/docs`，但当前阶段不必急着调整。

## 19. 第一阶段实现范围建议

第一阶段只实现最小可运行后端：

- 创建 4 人游戏。
- 固定随机种子初始化。
- 查询状态。
- 查询合法动作。
- 提交动作。
- 完整跑通一局 4 人游戏。
- 使用内存保存游戏。
- 提供事件日志。
- 支持强制放置，不提供放置阶段放弃动作。
- 支持自动抵押、破产状态和偷渡客放置。
- 支持海盗身份动态变化。
- 支持每局串行处理、不同局并行处理。
- 提供随机 AI 或测试脚本做自动对局。
- 提供固定随机种子的重置/新建游戏能力，方便 Python AI 训练环境接入。

暂不实现：

- 图形界面。
- 账号系统。
- 联机大厅。
- 断线重连。
- 数据库持久化。
- 复杂 AI。
- 3 人或 5 人规则。

## 20. 已确认的复杂规则口径

以下规则已经确认，后续实现按本节口径处理。

### 20.1 强制抵押自动处理

已确认：强制支付时，如果现金不足，系统自动抵押股份。

处理方式：

- 按系统最方便的稳定顺序选择可抵押股份。
- 每张抵押股份获得 12 比索。
- 抵押过程不进入玩家选择阶段。
- 玩家和 AI 不提供主动抵押动作。
- 玩家和 AI 不提供主动赎回动作。
- 终局时未赎回抵押股份照常扣 15。
- 如果终局玩家财富为负，允许得到负分。

原因：

主动抵押从贪心角度没有必要。抵押股份仍属于玩家，终局仍会计算该股份市值。未赎回贷款只按张数扣分，因此抵押哪一种货物股份不影响最终财富。

### 20.2 竞拍阶段支付

竞拍阶段不允许主动抵押。出价时按最大支付能力校验，成交后如果现金不足，由系统自动抵押补足支付。

原因：

这样既保留了股份贷款带来的支付能力，又避免为贷款增加额外玩家动作。

### 20.3 破产状态与偷渡客

已确认：破产状态由系统自动进入。玩家进入破产状态需要同时满足：

- 没有可以抵押的未抵押股份。
- 当前现金低于当前局面下任何一个可行放置操作的费用。

破产状态一旦进入，本轮航行内无法解除。即使玩家之后获得现金，本轮仍保持破产状态。

破产状态下，玩家只能作为偷渡客放置到货船，并花掉所有剩余现金。

偷渡客选择限制：

- 只能选择费用最低的可用货船。
- 如果有多个费用最低的货船，选择 ID 最小的船。

原因：

这样能保持该规则作为破产保护，而不是让有资产玩家故意少付。本轮锁定也能避免玩家通过后续收益反复进出破产状态，让前端和 AI 面对的合法动作更稳定。

### 20.4 海盗身份动态变化

已确认：海盗棋子登船后变为普通船员，不再保留海盗身份。

处理方式：

- 海盗船长登船后，如果副手仍在海盗船上，副手自动成为新船长。
- 如果海盗船上没有剩余海盗，后续不再发生海盗掠夺。
- 第 3 次移动后的劫掠、目的地选择和海盗收益，只与当时仍留在海盗船上的新任船长及其他海盗有关。

## 21. 剩余不确定问题

当前没有已知会阻塞第一版实现的规则问题。

已确认边界：

- 破产玩家如果没有可用货船空位，自动场外偷渡到 ID 最小的本轮出航船，不占位置，并花掉所有剩余现金。
- 场外偷渡者不参与货船收益平分；普通船员先正常平分，然后每名场外偷渡者获得等同于普通单份收益的金额。
- 非破产玩家如果在放置阶段没有任何合法位置，视为程序错误。当前 4 人规则下版图容量应足够容纳所有棋子。

## 22. 推荐下一步

建议下一步先确认第 21 节的边界问题，然后进入后端第一版拆分：

1. 定义领域模型。
2. 定义阶段枚举和动作枚举。
3. 实现合法动作生成。
4. 实现动作执行和状态流转。
5. 实现一轮结算。
6. 增加 REST 接口。
7. 用随机 AI 跑完整局测试。
